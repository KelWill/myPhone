//so, the best way to do fb stuff might be through a combination of
//email updates to a fbspecfic email address that I can then
//email responses from. Alternatively, I could mess around with a database

exports.handle = function(message, response){
  port = port || 'I guess I don\'t have access';
  console.log(port);
  response.end();
};

var get = function(message){

};

var getMessages = function(message){

};

var getPosts = function(message){

};

var getComments = function(message){

};
